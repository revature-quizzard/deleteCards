export class GameSettings {

    maxPlayers: any;
    matchTimer: any;


    constructor(maxPlayers: any , matchTimer : any) {
        this.maxPlayers = maxPlayers;
        this.matchTimer = matchTimer;
    }

}